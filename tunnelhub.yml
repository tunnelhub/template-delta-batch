service:
  type: automation
  uuid: ${automationUuid}

package:
  artifact: dist/artifact.zip

configuration:
  runtimeEngine: LAMBDA
  entrypoint: index.handler
  runtime: nodejs20.x
  memorySize: 256
  timeout: 30