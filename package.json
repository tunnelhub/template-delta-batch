{
  "license": "UNLICENSED",
  "main": "src/index.ts",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --collect-coverage --silent",
    "test:badges": "jest --coverage --silent && make-coverage-badge",
    "tsc": "tsc",
    "build": "tunnelhub-sdk-esbuild",
    "deploy:dev": "pnpm run build && th deploy-automation --env DEV",
    "deploy:prd": "pnpm run build && th deploy-automation --env PRD"
  },
  "devDependencies": {
    "@tybys/cross-zip": "^3.1.0",
    "@types/aws-lambda": "^8.0.0",
    "@types/jest": "^29.0.0",
    "esbuild": "^0.28.0",
    "esbuild-plugin-copy": "^2.1.1",
    "glob": "^13.0.6",
    "jest": "^29.0.0",
    "js-yaml": "^4.1.0",
    "make-coverage-badge": "^1.2.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@tunnelhub/sdk": "^4.1.0"
  },
  "packageManager": "pnpm@11.5.2"
}
